{
  "name": "@chronicler/types",
  "version": "0.0.0",
  "description": "Shared TypeScript types for Chief Chronicler project",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
   "dependencies": {
    "typescript": "^5.0.0"
  }
}