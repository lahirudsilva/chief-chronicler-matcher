// Export the router and types for frontend consumption
export { appRouter } from './router';
export type { AppRouter } from './router';

// Export the services if needed
export * from './services/locationMatcher';